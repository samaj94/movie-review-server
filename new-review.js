import handler from "./libs/handler-lib";

export const main = handler(async (event, context) => {
  const data = JSON.parse(event.body);
  // CHeck for movie or create new one
  // Add Review

  return params.Item;
});